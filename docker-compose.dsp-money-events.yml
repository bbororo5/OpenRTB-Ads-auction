services:
  dsp-money-seoul:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: rtb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: local-dev-postgres-password
    ports:
      - "35532:5432"
    volumes:
      - ./infrastructure/postgres/dsp-money-events/schema.sql:/docker-entrypoint-initdb.d/001-schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d rtb"]
      interval: 2s
      timeout: 2s
      retries: 15

  dsp-money-tokyo:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: rtb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: local-dev-postgres-password
    ports:
      - "45532:5432"
    volumes:
      - ./infrastructure/postgres/dsp-money-events/schema.sql:/docker-entrypoint-initdb.d/001-schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d rtb"]
      interval: 2s
      timeout: 2s
      retries: 15
